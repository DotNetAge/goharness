# TODO


- [ ] 增加 Permission的Deny魔术词，在客户端点击“拒绝”时发给后端，这与发送“同意”给后端的流程是一样的，而不是戛然而止（点击取消可以不发送，直接停止处理），要让Agent知道用户不同意授权，要想其它的办法进行处理。
- [ ] 在注册工具时，如果模型的`IsLocal=true`, 则 `SubAgent`, `TeamXXX` 等工具就不需要注册了。国为本地模型无法执行多线程的Agent处理。