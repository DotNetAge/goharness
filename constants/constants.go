package constants

// Default configuration values
const (
	// Reactor defaults
	DefaultMaxSteps = 200

	// Thinker defaults
	DefaultMaxTokens   = 4096
	DefaultTemperature = 0.7
)

// Special values
const (
	DefaultModel = ""
)
