package constants

// Default configuration values
const (
	// Reactor defaults
	DefaultMaxSteps = 200

	// Thinker defaults
	DefaultTemperature = 0.7
)

// Special values
const (
	DefaultModel = ""
)
